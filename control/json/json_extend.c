#include <stdio.h>
#include <stdlib.h>
#include <memory.h>
#include "../../mqtt/mqtt.h"

void broker_control_info(char * control_type){
    char * type = "json";
    strcpy(control_type, type);
}

int broker_control_strat(void){
    
    return 0;
}